source "https://rubygems.org"

# Use GitHub Pages supported dependency set
gem "github-pages", group: :jekyll_plugins

# Default theme
gem "minima"

# Windows timezone support
platforms :mingw, :x64_mingw, :mswin do
  gem "tzinfo-data"
end